## What and why

<!-- The problem, what actually changed, the result, and why this approach. -->

## How it was verified

<!-- What you ran and what it showed — and what you did not verify. A behaviour or bug change comes with the test that catches it (for a bug, the one that reproduces it before the fix), or say here why not. A review run in the same session that wrote the change is not an independent review; do not describe it as one. Put user-facing explanation and history where it belongs: README, CHANGELOG, or the page that covers it. -->

<!--
Then these two, in this order, at the end and with nothing after them:

  Closes #N                     Only when this pull request completes the issue.
                                `Part of #N` for partial work, `Related to #N`
                                otherwise; omit the line when there is none.
                                From another repository: `owner/repo#N`.
                                `Closes` auto-closes, so the wrong verb closes
                                unfinished work.

  Assisted-by: <agent>:<model>  When AI wrote or assisted, e.g.
                                `Assisted-by: Claude:claude-fable-5-1`. Keep any
                                existing ones: several form one block at the end.

This repository's CONTRIBUTING.md has the rest. Delete these comment lines —
GitHub keeps them in the squash commit.
-->
